{{- if and .Values.rollout.canary.enabled .Values.rollout.canary.canaryIngress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "service-chart.name" . }}-canary-ingress
  namespace: argo-rollouts
  {{- if or .Values.ingress.annotations .Values.rollout.canary.canaryIngress.annotations }}
  annotations:
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.rollout.canary.canaryIngress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    - host: {{ include "service-chart.canaryHost" . }}
      http:
        paths:
          - path: {{ .Values.rollout.canary.canaryIngress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "service-chart.canaryServiceName" . }}
                port:
                  number: 80
{{- end }}
