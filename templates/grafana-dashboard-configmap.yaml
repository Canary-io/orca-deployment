apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-json
  namespace: monitoring
data:
  dashboard.json: |
    {
      "dashboard": {
        "id": null,
        "title": "Canary Metrics"
      },
      "overwrite": true
    }
